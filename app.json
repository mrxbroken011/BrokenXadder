{
  "name": "BrokenXadder",
  "description": "simpe members adder bot",
  "repository": "https://github.com/mrxbroken011/Brokenxadder",
  "env": {
    "BOT_TOKEN": {
      "description": "Telegram Bot Token",
      "required": true
    },
    "OWNER_ID": {
      "description": "Telegram User ID of the bot owner",
      "required": true
    }
  },
  "buildpacks": [
    {
      "url": "https://github.com/heroku/heroku-buildpack-python"
    },
    {
      "url": "https://github.com/jonathanong/heroku-buildpack-ffmpeg-latest"
    },
    {
      "url": "https://github.com/heroku/heroku-buildpack-nodejs"
    }
  ],
  "formation": {
    "web": {
      "quantity": 1,
      "size": "free"
    }
  },
  "environments": {
    "production": {
      "BOT_TOKEN": {
        "description": "Telegram Bot Token for production environment",
        "required": true
      },
      "OWNER_ID": {
        "description": "Telegram User ID of the bot owner for production environment",
        "required": true
      }
    }
  }
}
